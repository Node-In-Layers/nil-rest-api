import { ServicesContext } from '@node-in-layers/core'

import { ExpressServices } from './types.js'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const create = (context: ServicesContext): ExpressServices => {
  return {}
}

export { create }
